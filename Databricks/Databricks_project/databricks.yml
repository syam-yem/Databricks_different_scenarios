# This is a Databricks asset bundle definition for Databricks_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Databricks_project
  uuid: fbe90899-2755-4371-933c-400eb0c59190

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2365362884168028.8.azuredatabricks.net:443

  prod:
    mode: production
    workspace:
      host: https://adb-2365362884168028.8.azuredatabricks.net:443
      # We explicitly deploy to /Workspace/Users/syammangat2001@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/syammangat2001@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: syammangat2001@gmail.com
        level: CAN_MANAGE
